package io.github.amayaframework.core.handlers;

/**
 * An enum containing the main events of the framework that can be responded to.
 */
public enum Event {
    SERVER_STARTING,
    SERVER_CLOSING,
    INPUT_ERROR,
    OUTPUT_ERROR
}
